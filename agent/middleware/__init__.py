"""Agent middleware package.

Middleware modules that plug into ``langchain.agents.create_agent`` live here.
M2 only introduces the package scaffolding; concrete middlewares land in M3
(``TokenTrackingMiddleware``) and M5 (booking-specific middleware stack).
"""
