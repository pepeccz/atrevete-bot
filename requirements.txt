# Agent & LLM
langgraph==1.1.9
langgraph-checkpoint-redis==0.4.1  # TODO: venv reinstall before Phase 7
langchain==1.2.15
langchain-core>=1.2.5  # CVE-2025-68664: critical prompt injection patched in 1.2.5
langchain-openai>=0.3.0
langfuse>=2.0.0

# API & Web Framework
fastapi[standard]==0.116.1
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4  # Used by api/routes/admin.py (passlib API); shared/security.py uses bcrypt directly

# Database & ORM
psycopg[binary]>=3.2.0
asyncpg>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13.0

# Cache & Messaging
redis>=5.0.0
fakeredis>=2.20.0  # testing

# Data Validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Template Engine (v5.0 prescriptive FSM)
jinja2>=3.1.0

# Environment & Config
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
freezegun>=1.5.0

# HTTP Client
httpx>=0.27.0

# Admin Panel
django>=5.2.8  # CVE-2025-64459: critical SQLi/auth bypass patched in 5.2.8
gunicorn>=21.0.0
django-extensions>=3.2.0
django-jsoneditor>=0.2.0
django-import-export>=3.3.0
whitenoise[brotli]>=6.6.0
django-unfold>=0.40.0

# External Service Integrations
google-api-python-client>=2.150
google-auth-oauthlib>=1.0.0  # OAuth2 flow for admin panel Google Calendar connection

# Audio Transcription
groq>=0.11.0
pydub>=0.25.1
ffmpeg-python>=0.2.0
aiohttp>=3.9.0

# Utilities
tenacity
phonenumbers
schedule>=1.2.0
pytz>=2024.1  # Timezone conversion for Europe/Madrid
python-dateutil>=2.8.0  # ISO 8601 date parsing
dateparser>=1.2.0  # Multilingual natural language date parsing (es + en fallback)
PyYAML>=6.0.0  # YAML config loading (booking.yaml hot-reload)

# Billing & Invoicing
stripe>=8.0.0
weasyprint>=62.0

# Testing — VCR cassette playback
pytest-recording>=0.13
